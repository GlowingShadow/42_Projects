#include <...>

int		main(int ac, char **av)
{
	int n;
	char buf[64];

	n = atoi(av[1]);
	if (n + 4 <= 9)
	{

		memcpy(buf, av[2], n * 4);
		if (*(int*)(buf+40) == 1464814662) // FLOW
		{
			execl("/bin/bash", NULL);
		}
	}
	return (0);
}