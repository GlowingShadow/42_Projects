#include <...>


int		auth(char *login, unsigned int serial)
{
	int len;

	strcspn();
	len = strnlen(login, x);
	if (len > 5)
	{
		if (ptrace(0, 1, 0, 0) == -1)
		{
			puts("\033[32m.---------------------------.");
			puts("\033[31m| !! TAMPERING DETECTED !!  |");
			puts("\033[32m'---------------------------'");
			return 1;
		}
		i = *(login + 3) ^ 0x1337 + 0x5eeded;
		// ...
		// more awful calculation

		if (serial == i)
			return 0;
	}
	else
		return 1;
	return 0;	
}

int		main(void)
{
	char login[X];
	puts("***********************************");
	puts("*\t\tlevel06\t\t  *");
	puts("***********************************");
	printf("-> Enter Login: ");
	fgets(login, 32, stdin);

	unsigned int serial;
	puts("***********************************");
	puts("***** NEW ACCOUNT DETECTED ********");
	puts("***********************************");
	printf("-> Enter Serial: ");
	scanf(stdin, "%u", &serial);

	if (auth(login, serial) != 0)
	{
		puts("Authenticated!");
		system("/bin/sh");
	}
	else
		return 1;
	
	return 0;
}