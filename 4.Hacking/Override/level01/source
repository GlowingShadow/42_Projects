#include <...>

char a_user_name[X];

int		verify_user_name()
{
	puts("verifying username....\n");
	if (a_user_name == "dat_wil")
		return 1;
	return 0;
}
int		verify_user_pass(char *s)
{
	if (s == "admin")
		return 0;
	return 0;
}

int		main(void)
{
	char buf[Y];

	puts("********* ADMIN LOGIN PROMPT *********");
	
	printf("Enter Username: ");
	fgets(a_user_name, X, stdin);
	if (verify_user_name())
	{
		puts("Enter Password: \"Enter Password:");
		fgets(buf, Y, stdin);
		if (verify_user_pass(buf))
		{
		}
		else
			puts("nope, incorrect password...\n");
	}
	else
		puts("nope, incorrect username...\n\"nope, incorrect username...");
	return (0);
}